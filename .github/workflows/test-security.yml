name: Test Security Scan (Ollama)

on:
  workflow_dispatch:

jobs:
  test-ollama:
    # SECURITY: Restrict who can trigger this workflow
    if: ${{ contains(fromJSON('["andrefelippeadfarias"]'), github.actor) }}
    runs-on: self-hosted
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for security scans

      - name: Run Security Scan
        shell: powershell
        run: |
          .\scripts\test-security-ollama.ps1